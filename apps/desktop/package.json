{
  "name": "ebirforms-desktop",
  "private": true,
  "version": "0.1.1",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "check": "npm --prefix frontend install && npm --prefix frontend run check"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
